#include <stdio.h>
#include <stlib.h>
#include <time.h>

/**
 * main - entry point
 * return: Always 0 (Success)
 */

int main(0)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
	putchar(low)

	for (low = 'A'; low <= 'Z'; low++)
	putchar(low);
	putchar('\n');

	return (0);

}
